# Strategy — Docker framework stack

> Issue #41 / #48

Keep **compose canonical on HATH0R-CLI**; members ship workflow JSON + pointers. Shared Redis/NGINX edge; apps (ATC/MCP/UXP) on profile. Prefer `hath0r` CLI over ad-hoc docker scripts.
